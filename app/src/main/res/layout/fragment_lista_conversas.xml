<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootConversaFinalizada"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- Lista de conversas -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_conversas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        tools:listitem="@layout/item_conversa" />

    <LinearLayout
        android:id="@+id/layout_vazio"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <ImageView
            android:id="@+id/icone_vazio"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_chat_empty_end"
            android:contentDescription="Sem conversas"
            app:tint="#CCCCCC" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nenhuma conversa finalizada ainda"
            android:textColor="#999999"
            android:textSize="16sp"
            android:layout_marginTop="12dp" />
    </LinearLayout>

</FrameLayout>
