<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layoutRaiz"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- RecyclerView normal -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_conversas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        tools:listitem="@layout/item_conversa" />

    <LinearLayout
        android:id="@+id/layout_vazio"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp"
        android:visibility="gone">

        <ImageView
            android:id="@+id/icone_vazio"
            android:layout_width="420dp"
            android:layout_height="420dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/ic_chat_empty_waiting"
            android:contentDescription="Conversa vazia"
            android:scaleType="centerInside" />


    </LinearLayout>


</FrameLayout>
